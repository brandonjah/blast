= simple_form_for :tweet, url: tweets_path, remote: true, method: :post do |f|
  = f.input :message
  = f.simple_fields_for :user do |u|
    = u.input :id, as: :hidden
  = f.simple_fields_for :content do |u|
    = u.input :name, as: :select, collection: Content.all
  = f.submit "Send Tweet"