- if admin_user?
	= simple_form_for @content do |f|
		= f.input :name
		= f.input :post, input_html: { maxlength: 140 }
		= f.input :post_time, as: :datetime
		= f.button :submit
- else
	%h3 Sign in to create content	  