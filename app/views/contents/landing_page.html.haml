#user_nav 
  - if current_user
    %div
      Signed in as #{current_user.name}   #{link_to "Sign out", signout_path}
  - else
    = link_to "Sign in with Twitter", "/auth/twitter?origin=#{request.original_url}"

#content
  = render "/contents/name/#{@landing_page}"