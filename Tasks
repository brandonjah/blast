TASK LIST

- sign in with twitter sends them back to the page they were on

- test to see that tweets are actually sent when scheduled
- add validation for required fields


IDEAS

- add email signup to notify users of similar events (categorize events like 'music', 'los angeles')